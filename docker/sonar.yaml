#
# This file run sonar qube
#
# Important:
# This configuration is intended for development purpose
#
version: '3.8'
services:
  h5adar-sonar:
    image: sonarqube:latest
    environment:
      - sonar.forceAuthentication=false
    ports:
      - 127.0.0.1:9000:9000
